# Elastic search console app

## To run:
- Install dependencies: `go install`
- Fill `.env` environment variables using reference `.env.example`
- Build and run database: `make db-up`
- Run migrations: `make migration`
- Run: `make run`
